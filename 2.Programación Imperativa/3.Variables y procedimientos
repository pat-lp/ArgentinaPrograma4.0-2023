Ejercicio 1: ¿Y el tablero?
Usando la consola, decí con tus propias palabras qué hace la función funcionMisteriosa, que recibe dos números enteros como argumentos.

Resolución: 
Te presentamos un desafío: usando la consola, decí con tus propias palabras qué hace la función funcionMisteriosa, 
que recibe dos números enteros como argumentos.
¡Vas a ver que podés averiguarlo sin tener un tablero!


Ejercicio 2: Impresión por pantalla
Probá funcionEgocentrica en la consola.
function funcionEgocentrica() {
  imprimir("soy una función que imprime por pantalla");
  imprimir("y estoy por devolver el valor 5");
  return 5;
}

Resolución: 
funcionEgocentrica()

Ejercicio 3: Martin Fierro
Definí la function versosMartinFierro que imprima por pantalla los primeros versos del Martín Fierro

Resolución: 
function versosMartinFierro(){
  imprimir("Aquí me pongo a cantar");
  imprimir("Al compás de la vigüela;");
  imprimir("Que el hombre que lo desvela");
  imprimir("Una pena extraordinaria");
  return 0;
}

Ejercicio 4: ¿Y los procedimientos?
Envía esta nueva versión de versosMartinFierro

Resolución: function versosMartinFierro() {
    imprimir("Aquí me pongo a cantar");
    imprimir("Al compás de la vigüela;");
    imprimir("Que el hombre que lo desvela");
    imprimir("Una pena extraordinaria");
}

Ejercicio 5: ¿Y el program?
Copiá y enviá este programa

Resolución: 
imprimir("Tirando dados");
imprimir("La primera tirada dio " + tirarDado());
imprimir("La segunda tirada dio " + tirarDado());
imprimir("La tercera tirada dio " + tirarDado());

Ejercicio 6: Coerciones
definí una función elefantesEquilibristas, que tome un número de elefantes y devuelva una rima de una conocida canción:

Resolución: 
function elefantesEquilibristas(num){
  return num + " elefantes se balanceaban"
}

Ejercicio 7: El círculo de la vida
Definí las funciones perimetroCirculo y areaCirculo que reciben el radio de un círculo y a partir del mismo nos retornan su perímetro y su área.

Resolución: 
function perimetroCirculo(radioCirc){
  return (3.14159265358979 * 2 ) * radioCirc
}

function areaCirculo(radioCirc){
  return 3.14159265358979 * (radioCirc * radioCirc)
}

Ejercicio 8: PIenso que así es más fácil
Cambiá los lugares donde aparece 3.14159265358979 por la variable pi en las funciones que tenemos definidas.

Resolución: 
let pi = 3.14159265358979;

function perimetroCirculo(radioCirc){
  return (pi * 2 ) * radioCirc
}

function areaCirculo(radioCirc){
  return pi * (radioCirc * radioCirc)
}

Ejercicio 9: Esto no tiene valor
Tenemos esta función definida:

function sumaSinSentido() {
  return numero + 8;
}
Probala en la consola y fijate qué sucede.

Resolución: 
sumaSinSentido()

Ejercicio 10: Variables globales
Definí una función ascensorSobrecargado, que toma una cantidad de personas y retorna si entre todas superan la carga máxima de 300 kg.

Resolución: 
let cargaMaximaEnKilogramos = 300;
function ascensorSobrecargado(cantPersonas){
  return (pesoPromedioPersonaEnKilogramos * cantPersonas) > cargaMaximaEnKilogramos; 
}

Ejercicio 11: La buena fortuna
Definí el procedimiento aumentarFortuna que duplique el valor de la variable global pesosEnMiBilletera.

Resolución: 
function aumentarFortuna(){
  pesosEnMiBilletera = pesosEnMiBilletera * 2;
} 

Ejercicio 12: ¿Y esto cuánto vale?
Dada la función:

function cuentaExtravagante() {
  let numero = 8;
  numero *= 2;
  numero += 4;
  return numero;
}

Si la invocaramos ¿qué crees que retornaría?
Veamos el paso a paso:
inicialmente la variable numero vale 8;
al hacer numero *= 2 la variable pasa a tener su valor multiplicado por 2, es decir, 16;
al hacer numero += 4 le sumamos 4 a 16 y lo guardamos en número, por ende la función cuentaExtravagante retorna 20.
